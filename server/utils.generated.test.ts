import { checkValidTime } from './utils';

describe('checkValidTime', () => {
  it('should expose a function', () => {
		expect(checkValidTime).toBeDefined();
	});
  
  it('checkValidTime should return expected output', () => {
    // const retValue = checkValidTime(inputTime);
    expect(false).toBeTruthy();
  });
});